syntax = "proto3";
package clients;

message GetClientRequest {
  string session_id = 1;
}

message GetClientResponse {
  string full_name = 1;
  string email = 2;
  int64 money_amount_in_cents = 3;
}

service ClientService {
  rpc GetClient(GetClientRequest) returns (GetClientResponse);
}